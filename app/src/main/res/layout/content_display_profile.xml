<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/activity_display_profile"
    tools:context="com.ericgithinji.profilesview.DisplayProfileActivity">
    
    <ImageView
        android:id="@+id/full_bleed"
        android:layout_width="match_parent"
        android:layout_height="280dp"
        android:src="@drawable/nana_ama"
        android:scaleType="centerCrop"
        />
    <TextView android:layout_height="280dp" android:layout_width="match_parent"
        android:id="@+id/name"
        android:text="Nana-Ama"
        android:textSize="40dp"
        android:textColor="@android:color/white"
        android:gravity="bottom|center"
        />

    <LinearLayout
        android:layout_below="@+id/full_bleed"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="20dp"
        android:layout_marginTop="50dp"
        android:layout_marginBottom="20dp"
        android:id="@+id/linearLayout">
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/phone"
            android:layout_gravity="center_vertical"/>
        <TextView
            android:id="@+id/phone"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/phone_number"
            android:textSize="25dp"
            android:textStyle="bold"
            android:paddingStart="30dp"
            android:layout_gravity="center_vertical" />
    </LinearLayout>

    <TextView
        android:id="@+id/bio"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/bio"
        android:textSize="25dp"
        android:textStyle="italic"
        android:textAlignment="center"
        android:layout_below="@+id/linearLayout"
        android:paddingLeft="20dp"
        android:paddingRight="20dp"/>

</RelativeLayout>
